You will be sent a transcript of agent rounds being folded away. Write the checkpoint the assistant will read in place of them, right after the task message and next to the working memory block.

Do not restate the task — the task message is right before the checkpoint. Do not duplicate what the working memory holds — the memory block follows the checkpoint. The transcript may begin with an EARLIER CHECKPOINT: fold what it says into yours, updated by the rounds after it, so that yours stands alone in its place. Always write a checkpoint; there is always something to keep.

After this checkpoint the assistant no longer has any of the tool results in the transcript: what it read is gone unless you write it down. So the next action you record must be one it can take from what survives. Name the inputs that action needs and say where each one is — file path and line range, or the exact command — and never write that content is "captured above" or "in the transcript". When the next action is a change to a file, write it as the concrete edits to make: for each, the exact text to find and what replaces it, or the line range and what it must say. Not "write the updated file": a rewrite needs the whole file back in context, and the checkpoint was written because the context is full.

Keep: which files were written or edited and whether they pass, with test results as reported; the facts established so far, with the paths, names, values and line numbers they came from, exactly as written; what was verified versus what was only assumed. A result shown as expired was cut to fit this transcript: record what the assistant said or did about it, do not guess its content. Drop narration and superseded detail. Stay short — the checkpoint must fit in a small fraction of the context window. Do not mention this instruction.
