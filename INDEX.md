# INDEX

_Auto-generated by `generate_index.sh`. Each section follows the workspace directory tree; each entry is a model-written description of the file's contents and how it is used._


## src/desh

### __init__.py

The file is an empty Python package initialization module that serves only to mark its directory as a Python package, containing no executable code, documentation, or imports.

### engine.py

The `engine.py` file implements a generic, abstract discrete-event simulation (DES) framework centered around three core components: a `State` abstract base class that serves as a placeholder for specific simulation states, an `Event` abstract base class that defines a priority level and an abstract `execute` method responsible for transforming the current state and generating new events, and the main `Engine` class that orchestrates the simulation loop. The `Engine` utilizes a priority queue based on a heap to process events in order of their assigned `Priority` (HIGH, NORMAL, or LOW), ensuring deterministic execution through sequence counters for tie-breaking. It provides three primary entry points for users—`run`, `iter`, and `trace`—which differ only in how they expose the simulation's progression, returning the final state, yielding intermediate states as a generator, or collecting all intermediate states into a tuple, respectively. A key feature of the engine is its transactional guarantee, where each step is executed atomically so that the state is only updated if the event's `execute` method completes successfully; if an exception occurs, the state remains unchanged. The engine includes robust error handling mechanisms through optional `on_error` and `on_interrupt` handlers that can recover from exceptions or `KeyboardInterrupt` by returning lists of replacement events, with distinct outcomes like `RECOVERED`, `INTERRUPTED`, or `HANDLER_ERROR` depending on the failure mode. Additionally, the module supports structured logging to a JSONL file and human-readable debug output to stderr, recording detailed metadata for each step such as the event type, payload, emitted events, execution duration, and the final outcome, while also defining specific exception types like `SimulationError` and `HandlerError` to manage contract violations and handler failures.

### render.py

The `render.py` file defines the terminal styling and output formatting utilities for the application, beginning with a bounded `reprlib.Repr` instance configured to truncate long representations. It introduces a `Palette` class that encapsulates ANSI escape sequences for various semantic colors—such as errors, warnings, user and assistant chrome, diff contexts, and Python syntax elements—and provides a callable instance that applies these colors only when the target stream is a TTY. Two module-level instances, `c_out` and `c_err`, are initialized for standard output and standard error, respectively. The file also contains a `Gutter` class that wraps an output stream to prepend a configurable prefix to the start of every line, carefully managing state across arbitrary write chunks to handle line breaks and preserve SGR color state for multi-line content. Finally, it provides helper functions `rl_prompt`, which formats text with readline-specific escape codes for prompt coloring, and `pretty_log`, which formats a log record dictionary into a human-readable string including payload, emitted items, depth, duration, and optional outcome warnings.

### tools.py

This module implements a tool registry system that bridges the gap between Python callables and the JSON schemas required by language model APIs, residing within the engine package to serve both transport and execution layers. It provides a `Tool` dataclass that encapsulates a single function, its OpenAI-shaped schema, and metadata such as confirmation policies, parameter injection lists, and custom rendering hooks for previews and result folding. A key feature is the automatic derivation of JSON schemas from Python function signatures, type hints, and Google-style docstrings via the `Tool.define` classmethod, which ensures the model's view of available arguments strictly matches the function's actual interface while allowing manual overrides for complex cases. The `ToolRegistry` dataclass manages an immutable collection of tools, offering methods to register new tools, retrieve their schemas for API requests, and calculate canonical argument identities for repetition detection. The `invoke` method handles the execution of requested tools by parsing JSON arguments, validating them against the function signature, injecting harness-provided parameters, and capturing any exceptions or type errors as recoverable text responses rather than raised exceptions, while also enforcing a maximum character limit on results to prevent context window overflow.


## src/desh/llama

### __init__.py

The file is empty.

### esc_watcher.py

The file defines an `ESCWatcher` class that monitors standard input for ESC keypresses to provide an interrupt mechanism, likely for stopping an LLM process. It utilizes POSIX-specific modules like `termios` and `tty` to switch the terminal into cbreak mode, allowing the watcher to read characters without waiting for a newline, and it explicitly notes that this functionality is restricted to POSIX systems because the dependencies will fail on Windows. The class implements the context manager protocol, ensuring that the terminal settings are saved upon entry and restored upon exit, while a daemon thread runs a loop that uses `select` to check for available input with a short timeout. When an ESC byte is detected, the code waits for a brief grace period to determine if additional bytes arrive, which would indicate an escape sequence such as an arrow key that should be drained and ignored, or if the input remains silent, which confirms a bare ESC press and sets an internal `interrupted` flag to signal the caller.

### logger.py

This file defines a `Logger` class responsible for persisting completion data to a JSON Lines file, ensuring that the output format remains compatible with existing shell scripts and `jq` queries by maintaining a specific layout of timestamp, port, payload, and response. The class constructor accepts a file path which is expanded and stored for future use, while the `record` method constructs a dictionary from a `Request` and `Completion` object, creates necessary parent directories, and appends the serialized JSON record to the log file. The implementation explicitly excludes error states from being logged, adhering to a design principle that each record represents a single successful completion.

### pyscan.py

The `pyscan.py` file implements a streaming Python syntax scanner designed for a renderer that processes text in arbitrary fragments. It defines a pure function, `scan`, that takes a `ScanState` and incoming text to produce a list of colored spans, classifying Python code into categories such as keywords, builtins, calls, strings, numbers, comments, and default code. The scanner operates on a "decisive" rule, holding back tokens like identifiers, numbers, or partial string delimiters in a `pending` field within the `ScanState` until they can no longer be altered by subsequent input. The module includes helper functions to identify identifier runs, determine opening string delimiters (handling single and triple quotes), and scan through string contents while accounting for escape sequences and unterminated lines. It also provides a `finish` function to flush any remaining pending state at the end of a code block, ensuring that the scanner remains stateful yet pure across multiple calls, allowing the renderer to apply syntax highlighting incrementally without knowing the full context of the code fence in advance.

### send_direct.py

This file implements a command-line interface for interacting directly with a local `llama-server` instance, serving as a Python port of a previous bash prototype. It defines a `main` function that parses arguments for connection details, prompt configuration, and output formatting, allowing users to specify parameters such as temperature, maximum tokens, and system or user prompts. The script supports reading prompts from external files via a helper function that formats them into numbered blocks, and it constructs a `Request` object using the `desh.llama` package components. During execution, it handles both standard and streaming request modes; in streaming mode, it utilizes a rendering chain of `Seam`, `CodeFence`, and `Terminal` stages to display reasoning and code with specific styling, while non-streaming requests print the final content. The tool also provides options for dry-run operations that display the JSON payload without sending it, logging telemetry to a JSONL file, and directing output to a file or printing the full completion JSON.

### server.py

This file defines the `LlamaServer` class, which serves as a transport layer for communicating with a single `llama-server` instance in either single-model or router mode. It provides methods for non-streaming completions, streaming completions, retrieving model lists, fetching server properties, and determining maximum context sizes by parsing command-line arguments from model status data. The module establishes a specific error taxonomy with `LlamaUnreachable` for connection failures and timeouts, and `LlamaServerError` for structured error responses from the server, including those encountered mid-stream. A key feature is the `stream` method, which acts as a "T-junction" that processes Server-Sent Events frames simultaneously for constructing a final `Completion` object and for feeding channel-based text events to a `Renderer` protocol object, while also supporting a cancellation callback to halt the stream.

### stages.py

This file defines a chain of processing stages for rendering streaming text completions, where each stage transforms events and passes them to the next stage in the pipeline until they reach a final sink. The `Stage` base class establishes the contract for `feed` and `flush` methods, while specific implementations handle distinct aspects of the output: `ToolProgress` converts tool call metadata into a visible progress line that updates in place as arguments stream in; `PyHighlight` uses a streaming scanner to tokenize Python code and assign specific color channels to different token types; `CodeFence` acts as a state machine that detects markdown code fences in the content stream, holding back text to handle partial fence markers and re-channeling the body of Python fences to a specific `code_py` channel for downstream highlighting; and `Seam` inserts a visual separator between reasoning and content sections. The `Terminal` class serves as the final sink, writing text to a standard output stream with ANSI color codes applied based on the event channel, and it includes logic to handle carriage returns for in-place line updates on TTY devices. These stages are designed to be chained together, typically with `Seam` as the outermost stage and `Terminal` as the innermost sink, to transform raw wire events into formatted, colored terminal output.

### tokens.py

The file `tokens.py` implements token accounting logic for managing context window budgets within an LLM application, operating under two distinct regimes: a pre-completion estimation phase and a post-completion pricing phase. The `estimate_tokens` function provides a lightweight, character-based heuristic—approximating four characters per token—to estimate the size of text before it has been processed by a tokenizer, which is useful for clamping generation budgets and calculating system prompt shares. The `turn_tokens` function serves as the primary mechanism for pricing individual conversational turns for history management, utilizing server-provided usage data when available to accurately attribute prompt and completion tokens. When server usage data is missing, such as during cancelled streams or with older server versions, the function falls back to character-based estimates for both user and assistant messages. It specifically adjusts the completion token count to isolate the tokens used for the assistant's visible content from any reasoning content, ensuring that only the text that will be re-sent in future prompts is counted toward the turn's stored token budget.

### wire.py

This file defines the data structures and parsing utilities for interacting with a llama-server chat completion interface, specifically modeling the request payload, the resulting completion object, and the handling of Server-Sent Events (SSE) streams. The module introduces a `Request` dataclass that encapsulates parameters such as messages, model selection, and tool definitions, providing a `payload` method to construct the JSON body for the `/v1/chat/completions` endpoint by conditionally including optional keys based on the configuration. It also defines a frozen `ToolCall` dataclass to represent individual tool invocations and a `Completion` dataclass that stores the final state of a chat response, including content, reasoning, usage statistics, and tool calls. The `Completion` class offers two classmethods for construction: `from_response` parses a standard non-streaming JSON response, while `from_frames` aggregates a series of parsed SSE frames into a single completion object by accumulating text content, reasoning, and tool call arguments across multiple chunks. Additionally, the file provides two pure helper functions: `parse_sse` iterates over raw input lines to extract and parse JSON data from SSE frames, stopping at the `[DONE]` sentinel, and `events` translates a single parsed frame into a stream of channel-text pairs for rendering purposes, distinguishing between reasoning, content, and tool-related updates. This module is designed to be dependency-free regarding I/O and other project components, serving as a pure data and parsing layer for the application's server communication.


## src/desh_chat

### __init__.py

This file is an empty Python module initializer that serves to mark its containing directory as a package, though it currently defines no classes, functions, or variables.

### cli.py

The file `cli.py` serves as the primary entry point and command-line interface for a chatbot application built on a discrete-event simulation engine. It defines a `main` function that parses extensive command-line arguments to configure model parameters, logging destinations, session persistence, and optional tool capabilities such as file reading, writing, editing, bash execution, delegation to subagents, and scratchpad management. The module includes helper functions like `build_tools`, which constructs a `ToolRegistry` by conditionally adding specific tools based on user flags and wiring up delegate subagents with shared inference settings, and `resolve_session_file`, which determines the file path for chat history persistence based on explicit session paths or a designated folder structure. The `main` function orchestrates the initialization of a `LlamaServer` client, an `InferenceEngine`, and a `ChatState` object that aggregates settings, history, tools, and a scratchpad if enabled. Finally, it prints a detailed configuration summary to the console and launches the `Engine` to run the chat loop, seeding it with session loading and turn-start events while passing in error and interrupt handlers.

### coding.py

The `coding.py` file defines the core toolset for a coding agent, encapsulated within a `Workspace` dataclass that enforces strict path confinement to a specific project root by resolving paths, rejecting symlinks, and ensuring all operations remain within the designated directory. It provides four primary methods—`read`, `write`, `edit`, and `bash`—which serve as the interface for the agent to interact with the file system and execute shell commands, with `read` operating without user confirmation while `write`, `edit`, and `bash` require operator approval. The `bash` method uniquely mandates a `reason` argument before the command to provide context for the confirmation prompt, while the `edit` method performs precise string replacements with options for handling multiple occurrences. Additionally, the module includes an `edit_preview` function that generates a colored unified diff for user review and a `coding_registry` factory function that constructs a `ToolRegistry` instance, registering the workspace methods with specific configuration flags such as confirmation requirements, preview handlers, and identity definitions for repetition guarding.

### commands.py

This module defines the command system for the chat interface, establishing a central registry that unifies command completion, dispatch, and help listings. It introduces a `Command` event class that extends the base `Event` type, along with a `CommandSpec` dataclass to store metadata and handlers for each command. The `Command` class implements an `execute` method that routes user input to specific handler methods, such as `_cmd_compact` for history management, `_cmd_model` for switching inference models, and `_cmd_temperature` for adjusting generation parameters. It also includes utility methods for parsing and validating arguments, ensuring inputs like integers and floats fall within specified ranges before updating the `ChatState`. The module exposes a `COMMANDS` dictionary as the single source of truth for the command surface, mapping canonical command names to their specifications, and constructs a `_COMMAND_INDEX` that includes aliases to facilitate efficient dispatching while keeping the import graph acyclic by delaying certain imports to call time.

### delegate.py

This file defines the `Delegate` tool, a mechanism that allows a parent agent to offload a self-contained subtask to a freshly instantiated subagent engine, which runs synchronously and returns only its final answer to preserve the parent's context window. The `Delegate` dataclass encapsulates the configuration for spawning these subagents, including the inference engine, tool registry, and session file management, while the `delegate` method constructs a new `ChatState` with a specific system prompt, optional scratchpad support, and a checkpoint mechanism for handling tool round caps. It manages the execution of the child engine by redirecting standard output through a visual gutter to distinguish subagent activity from the main conversation, and it derives a unique session file path for the subagent based on the parent's session. The file also includes utility functions such as `fold_brief` to truncate detailed task arguments in the conversation history, `child_session_file` to generate subagent log names, and `answer` to interpret the subagent's final state—handling cases like context overflow, operator cancellation, or hitting tool round limits—and `_checked` to optionally append the results of a post-execution shell command to the final response.

### display.py

The `display.py` file defines a collection of leaf-level event classes that serve as terminal sinks for the application's event graph, each inheriting from a `DisplayEvent` base class configured with high priority to ensure output appears before subsequent processing steps. These frozen dataclass events, including `Info`, `Warn`, and `Error`, handle the printing of colored text messages to the console using a palette and rendering utility, while `DisplayHistory` iterates through the chat state's conversation turns to print either user summaries or color-coded transcript lines distinguishing between user and assistant input. Additionally, the `DisplayStats` event calculates and displays the current context window utilization and total session token counts by accounting for pending tokens, providing real-time feedback on resource usage without modifying the chat state or emitting further events.

### events.py

The file `events.py` implements the core event-driven state machine for a chat application, defining a strongly connected loop of events that manage the lifecycle of user interactions, model inference, and tool execution. It begins with `MaybeRegenerate` and `TurnStart`, which handle the resumption of the chat loop and the initialization of new turns, respectively, while `PromptUser` manages user input acquisition, command parsing, and readline-based auto-completion for slash commands. The module then defines the turn execution logic, starting with `UserMessage` to populate the pending turn and `NextRound` to construct the LLM request by budgeting tokens, checking for necessary history compaction, and assembling the system prompt, history view, and current turn state. `StreamCompletion` executes the streaming inference request, monitoring for user cancellation via an ESC watcher, and routes the result to either `TurnEnd` or `AppendRound` based on the finish reason. When the model requests tools, `AppendRound` records the round and checks for round caps or repetitive loops, subsequently triggering `ExecuteToolCalls`, which iterates through each tool call, handles operator confirmation via the `gate` module, executes the tool or records denials, and manages scratchpad updates. The cycle concludes with `TurnEnd`, which freezes the pending turn into the history and persists the session, while `CompactHistory` is available to summarize the conversation history when the context window becomes constrained.

### gate.py

This module implements the operator-side interface for tool execution, providing the logic to display pending calls, capture user decisions, and generate appropriate feedback messages for the language model. It defines a frozen `Answer` dataclass to encapsulate the user's choice (yes, no, cancel, or auto) along with any optional guidance text provided during a refusal. The primary entry point, `ask`, handles interactive terminal input by temporarily switching to cbreak mode to read single keypresses for immediate decisions or falling back to standard line input for detailed messages, while carefully managing the readline history to prevent confirmation prompts from cluttering the conversation record. The module also includes utility functions for formatting tool interactions: `shorten` truncates and indents long text results for display, and `describe_call` renders the tool invocation in a human-readable format by parsing JSON arguments, delegating to a tool-specific `preview` method when available, or falling back to a generic key-value listing with syntax highlighting. These components work together to ensure the operator is always presented with a clear view of the intended action and can provide nuanced feedback that is subsequently relayed to the model, with specific constant strings defined to inform the model when a call is denied or skipped.

### handlers.py

The file defines custom event handlers for the desh_chat application, specifically implementing an AutoOff event class that disables auto mode by updating the ChatState settings, alongside two functional handlers for error and interrupt conditions. The on_error handler logs exceptions and determines whether to close an active turn as cancelled with a specific stop reason or simply emit a MaybeRegenerate event to return to the prompt, while the on_interrupt handler distinguishes between auto and manual modes, either disabling auto mode and closing any in-progress turn or emitting an info message and an Exit event to terminate the application. These handlers interact with the underlying engine by returning lists of events that modify the chat state, ensuring that interrupted or errored turns are properly resolved before the user is returned to the input prompt.

### scratchpad.py

This file defines the data structures and logic for a persistent working memory system designed to mitigate context window limitations in multi-round tool interactions. It begins by defining a system prompt template that explains the mechanics of tool result expiration and the necessity of the scratchpad to the model, ensuring the agent understands when data will be lost. The module provides imperative utility functions that operate on a standard dictionary to add, remove, or clear entries, returning status messages suitable for tool call responses. To support immutable state management within the application's state object, it introduces a frozen dataclass that represents the scratchpad as an ordered tuple of key-value pairs, offering corresponding immutable update methods that return new instances rather than modifying existing ones. This class includes conversion methods to translate between the dictionary format used by the imperative functions and the tuple-based format used by the state object. Finally, the class provides rendering methods that format the current memory into a structured XML-like block, which includes metadata about the current round, the expiration threshold, and specific keys scheduled to expire, allowing the system to inject this context into user messages to guide the model's persistence behavior.

### session.py

This file implements the session persistence mechanism for a chat application, defining two high-priority events, `LoadSession` and `SaveSession`, that manage the restoration and writing of chat history to a local JSON file. The `LoadSession` event restores conversation turns from the specified session file, populating the system’s readline history and optionally recovering a scratchpad value if the tool is active, while handling missing or corrupt files by either starting a new session or moving the damaged file aside to prevent data loss. Conversely, the `SaveSession` event atomically writes the current history and metadata, such as the model and timestamp, to a temporary file before replacing the original to ensure that a crash does not leave a truncated session behind. A helper function named `persist` is also provided to generate the `SaveSession` event when a session file is configured, allowing other parts of the application to easily trigger persistence after history changes.

### state.py

This file defines the immutable data structures and logic for managing the state of a chat-based agent, serving as the central data model for the `desh` application. It introduces a `Settings` dataclass that configures model parameters, tool expiration, and context compaction thresholds, alongside a `COMPACTION_PROMPT` constant used to summarize long sessions. The core `ChatState` class extends a base `State` type, holding the current `ChatHistory`, inference engine details, and a `PendingTurn` that tracks the ongoing interaction between the user and the model, including methods to calculate token budgets and manage the scratchpad. Supporting this, the file defines `Round` and `ToolResult` classes to represent intermediate tool-call exchanges, with specific logic for "stubbing" expired tool results to optimize context usage, and a `Turn` class that finalizes completed interactions with serialization support. Finally, the `ChatHistory` class manages the sequence of `Turn` objects, providing utilities for viewing recent context within token limits, identifying turns since the last summary, and compacting the history into a synthetic summary turn to maintain performance within the model's context window.

### toolset.py

The file defines the default toolset for the chat-des application, establishing a pattern where tools are implemented as plain functions whose schemas are automatically derived from their signatures and docstrings to ensure consistency between the model's instructions and the actual function parameters. It specifically includes a `current_time` function that returns the current date and time as an ISO 8601 string, with an optional argument to adjust the timezone offset by a specified number of hours. The module also provides a `default_registry` function that returns a `ToolRegistry` instance pre-configured with the `current_time` tool registered with `confirm=False`, indicating that this specific operation is treated as read-only and does not require explicit operator confirmation before execution.

