# INDEX

_Auto-generated by `generate_index.sh`. Each section follows the workspace directory tree; each entry is a model-written description of the file's contents and how it is used._


## src/desh

### __init__.py

The file is empty, serving only as a package marker to designate its containing directory as a Python package without defining any additional structure or logic.

### engine.py

The `engine.py` file defines a generic Abstract Discrete-Event-Simulation (DES) framework built around three core abstractions: an empty `State` base class, an `Event` base class that carries a priority level and an abstract `execute` method, and a concrete `Engine` class that orchestrates the simulation loop. The engine uses a min-heap to manage a priority queue of events, ensuring that higher-priority events are processed before lower-priority ones, with sequence numbers breaking ties to maintain FIFO order within the same priority. It provides three primary entry points for executing simulations: `run` for retrieving only the final state, `iter` for yielding the state after each step as a generator, and `trace` for collecting all intermediate states into a tuple. The engine enforces a transactional guarantee where state updates are only committed if an event executes successfully; if an event raises a `KeyboardInterrupt` or a general `Exception`, the engine invokes optional `on_interrupt` or `on_error` handler callbacks, which must return a list of new events to push back into the queue, allowing for recovery or graceful termination. If handlers are not provided or if they themselves fail, specific exceptions like `SimulationError` or `HandlerError` are raised, determining the simulation's exit mode, which is classified as `drain` for normal completion, `propagate` for unhandled errors, or `halt` for handler failures. The module also includes robust logging capabilities that can write structured JSONL records to a file and human-readable debug output to standard error, capturing details such as step timing, queue depth, event payloads, and execution outcomes for every processed step.

### render.py

The file `render.py` provides utilities for terminal output formatting, including a `Palette` class that manages ANSI color codes for various UI elements like chrome, errors, and tool interactions, and a `Gutter` class that wraps a text stream to prepend a configurable prefix to the start of each line while tracking line state and color context across arbitrary write chunks. The module also defines a bounded `reprlib` instance for truncated string representations, a `rl_prompt` function for Readline-compatible prompt formatting, and a `pretty_log` function that formats log records with depth, duration, and outcome information. These components are used to structure and colorize console output, with the `Gutter` specifically designed to support nested execution contexts like subagents by maintaining indentation and preserving the ANSI color state of the content as it flows through the stream.

### tools.py

This file implements a tool registry system for the engine package, defining how Python callables are exposed to language models as OpenAI-shaped function schemas and how those functions are executed when requested. It provides a `Tool` dataclass that pairs a callable with its derived JSON schema, confirmation policy, and metadata for harness integration, along with a `ToolRegistry` that manages a frozen collection of tools, handles schema generation for API requests, and executes tool calls while strictly bounding result sizes and catching all exceptions to return recoverable text to the model. The module includes utility functions for deriving JSON schemas from Python type hints and parsing Google-style docstrings to extract parameter descriptions, ensuring that the function signature remains the single source of truth for the model’s available arguments. By treating policy as data on the tool rather than logic in the harness, it allows the transport layer to access schemas and the engine to invoke callables without being chat-specific, while enforcing that unannotated or unsupported parameters fail at definition time rather than during runtime execution.


## src/desh/llama

### __init__.py

The file is an empty `__init__.py` module, which serves solely to designate its parent directory as a Python package, enabling the import system to recognize the folder as a module namespace without introducing any additional code or configuration.

### esc_watcher.py

The `esc_watcher.py` file defines an `ESCWatcher` class that operates as a context manager to monitor standard input for Escape keypresses in a separate daemon thread, allowing for the interruption of long-running operations such as LLM interactions. It utilizes POSIX-specific modules like `termios` and `tty` to place the terminal in cbreak mode, enabling the capture of raw input without requiring a newline character. The core monitoring logic distinguishes between a standalone Escape press and multi-byte escape sequences (such as those generated by arrow keys or function keys) by employing a short grace period; if additional bytes are detected within this window, they are drained and ignored, whereas a lone Escape keypress sets an `interrupted` flag to signal the main thread. The class ensures proper cleanup by restoring the original terminal settings and joining the watcher thread upon exiting the context, relying on the caller to manage the lifecycle without automatically installing `atexit` handlers.

### logger.py

This file implements a `Logger` class within the `desh` project that persists completion events as JSON Lines records to a specified file path. The class is designed to maintain compatibility with existing data processing workflows by using a record layout that mirrors the output of a `send_direct.sh` script, specifically including fields for timestamp, port, payload, and response. Upon initialization, the logger expands the user-provided path, and its `record` method appends a new JSON object to the file after ensuring the parent directory exists. The method constructs the record by capturing the current timezone-aware timestamp, the target port, the request payload, and the completion response as a dictionary, while explicitly excluding errors to adhere to a design principle that one record represents exactly one successful completion.

### pyscan.py

This file implements a streaming Python syntax scanner intended for real-time code rendering, functioning as a pure state machine that processes text fragments incrementally to generate colored spans without requiring the complete source code. It defines a frozen `ScanState` dataclass to track whether the scanner is inside a string or comment and to hold back ambiguous tokens that await further input, such as partial identifiers, numbers, or string delimiters. The core logic resides in the `scan` function, which consumes a text fragment and returns a list of spans—tuples pairing a channel identifier like "py_kw" or "py_str" with the corresponding text—along with the updated state, while a `finish` function handles the end of a code block by flushing any remaining pending data. Supporting utilities include a `classify` function that assigns channels to identifiers based on whether they are Python keywords, built-ins, or user-defined calls, as well as helper functions to identify runs of characters and determine string delimiters, specifically handling the ambiguity between single and triple quotes. The design explicitly avoids full tokenization, focusing instead on highlighting strings, numbers, comments, and identifiers to provide sufficient visual context for a terminal renderer without tracking complex syntax like f-strings or decorators.

### send_direct.py

This file implements a command-line interface for interacting with a local llama-server instance, serving as a Python port of a previous bash prototype. It defines a `main` function that parses a comprehensive set of command-line arguments to configure request parameters such as port, temperature, token limits, and prompt sources, with the option to load prompts from external files formatted as numbered blocks. The script constructs a `Request` object and, if the no-op flag is set, prints the resulting JSON payload without sending it; otherwise, it instantiates a `LlamaServer` client to either stream the response through a rendering chain that applies syntax highlighting and terminal styling, or to retrieve a complete response in a single call. It handles specific exceptions for unreachable servers or server errors by printing messages to standard error, and it optionally records telemetry to a JSONL file via a `Logger` instance. Finally, the script outputs the result either as the full completion JSON, the raw content, or by writing the text to a specified file, returning an exit code of zero on success.

### server.py

The `LlamaServer` class in this module provides a Python transport layer for interacting with a single `llama-server` instance, supporting both single-model and router configurations through methods for non-streaming completions, streaming responses, model enumeration, property retrieval, and context length inspection. It defines a `Renderer` protocol for objects that can consume streamed text events and establishes a specific error taxonomy with `LlamaUnreachable` for connection failures and timeouts, and `LlamaServerError` for structured errors returned by the server either as HTTP responses or as mid-stream frames. The core `stream` method acts as a T-junction that simultaneously accumulates server-sent event frames for constructing a final `Completion` object and dispatches text events to a provided renderer, while also checking for cancellation signals to terminate the stream early. This file serves as a port of an earlier bash prototype and relies on helper functions from the `desh.llama.wire` module to handle request payloads, parse server-sent events, and transform frames into renderer-compatible events.

### stages.py

This file defines the renderer stage classes that form a chain for processing and displaying streaming completion events, starting with the base `Stage` class which establishes the `feed` and `flush` contract for passing events to the next link in the chain. It includes `ToolProgress`, which transforms tool name and argument events into a single dim line that updates in place to show the tool name and argument size, and `PyHighlight`, which uses a streaming scanner to break down Python code tokens and re-emit them on specific channels for syntax highlighting. The `Terminal` class acts as the final sink, writing text to a stream with channel-specific colors and handling carriage returns to clear previous line content on TTYs. Additionally, `Seam` inserts a separator line between reasoning and content sections, while `CodeFence` manages the state of code blocks by re-channeling text inside fences and holding back potential fence delimiters that might be split across data chunks.

### tokens.py

The `tokens.py` module provides utilities for estimating and accounting for token usage in an LLM-based system, distinguishing between pre-completion estimates and post-completion actuals. It defines `estimate_tokens`, which uses a simple heuristic of four characters per token to approximate the size of text before it is sent to a model, and `estimate_result_tokens`, which applies a denser heuristic of 3.3 characters per token specifically for tool results and code-like content. The `turn_tokens` function calculates the precise token cost of a conversation turn for history budgeting by leveraging the `usage` dictionary returned by the server; it isolates the token count attributable to the user message by subtracting known prior tokens from the total prompt tokens and apportions the completion tokens to the assistant's visible content versus its internal reasoning, falling back to character-based estimates if the server usage data is missing or yields non-positive results.

### wire.py

The `wire.py` file defines the core data structures and pure parsing utilities for interacting with a llama-server chat completion endpoint, specifically handling the request payload, the resulting completion object, and the streaming event stream. It begins with a `Request` dataclass that encapsulates parameters such as messages, model selection, temperature, and tool schemas, providing a `payload` method to construct the JSON body for the `/v1/chat/completions` API while conditionally including optional keys like `stream_options` and `seed` only when they are relevant to the server configuration. The file then defines a frozen `ToolCall` dataclass to represent individual function calls within a response, followed by a `Completion` dataclass that standardizes the output of both non-streaming and streaming requests; this class includes class methods `from_response` and `from_frames` to build the object from a direct JSON response or by folding parsed Server-Sent Event (SSE) frames, respectively, while also offering a `to_dict` method to serialize the completion back into a standard OpenAI-compatible format. Finally, the module provides two pure helper functions, `parse_sse` and `events`, which process raw SSE lines into JSON frames and translate those frames into channel-specific text events (such as reasoning, content, and tool arguments) for downstream renderers, ensuring that the frame shape is interpreted in a single, isolated location without any direct I/O operations or dependencies on other parts of the project.


## src/desh_chat

### __init__.py

The file is an empty Python module, serving as a placeholder to designate its parent directory as a Python package.

### cli.py

This file serves as the primary command-line entry point for a discrete-event simulation (DES) engine-based chatbot, orchestrating the initialization of the application environment, configuration of inference parameters, and the execution of the main event loop. It defines a comprehensive argument parser that accepts a wide range of options, including model selection, context window sizing, tool-specific flags for file operations and shell execution, session persistence settings, and logging preferences. The `main` function processes these arguments to construct a `LlamaServer` client, instantiate a `Settings` object, and build a `ToolRegistry` via the `build_tools` helper, which conditionally registers tools like Read, Write, Edit, Bash, and a `Delegate` for spawning subagents based on the provided flags. It also manages session file resolution through `resolve_session_file`, determining whether to load an existing JSON file or create a new one in a specified directory, and initializes a `ChatState` object that encapsulates the history, system prompt, scratchpad, and deadline constraints. The application runs by passing the initial state and a seed list of events, such as `LoadSession` and `TurnStart`, to the `Engine`, which processes the user's input or a predefined `--task` string. For one-shot task executions, the script exits with a specific status code determined by `task_exit_code`, which maps the stop reason of the final non-summary turn to a success or failure code, while interactive sessions simply return after the engine completes.

### coding.py

The `coding.py` file defines the core toolset for a coding agent, centered around the `Workspace` dataclass which encapsulates a project root and provides four primary methods: `read`, `write`, `edit`, and `bash`. These methods are designed to be safely confined within the specified root directory, enforcing path validation to prevent access outside the root or through symlinks, and they integrate with a `ToolRegistry` system that handles result size limits and user confirmation prompts for mutating operations. The `read` method implements sophisticated pagination and character-limit handling to return file contents without exceeding a defined size cap, while the `bash` method executes shell commands with a mandatory `reason` argument for transparency and spills large outputs to temporary files within the root for later retrieval. Additionally, the module includes utility functions such as `fold_written` and `fold_edited` to compress large argument values in the model's context history, an `edit_preview` function that generates a colored unified diff for user confirmation, and a `coding_registry` factory function that assembles these tools into a configured registry with specific settings for confirmation, target extraction, and identity tracking.

### commands.py

This file defines the command processing layer for the chat interface, establishing a central registry and an event class that handles user-input commands. It introduces a `Command` event that inherits from a base `Event` type, containing the command name and arguments, and features an `execute` method that dispatches the command while catching `CommandError` exceptions to return user-friendly warnings. The module implements a series of handler methods for specific commands such as adjusting context limits, setting models, managing tool rounds, and toggling auto or thinking modes, each of which validates arguments and updates the chat state or emits display events. To support these operations, the file includes utility methods for parsing and validating integer and float arguments within specified bounds. Finally, it provides a `COMMANDS` dictionary that serves as the single source of truth for command definitions, descriptions, and handlers, alongside a derived `_COMMAND_INDEX` that maps both canonical names and aliases to their corresponding specs for efficient dispatch.

### delegate.py

This module implements the `Delegate` tool, which enables the main model to offload self-contained subtasks to isolated subagents that run synchronously within the parent's execution step. The file defines the `Delegate` dataclass, which holds the configuration for spawning child `Engine` instances, including the inference backend, tool registry, and session file path, while ensuring that the subagent inherits the parent's current settings but starts with a fresh `ChatState` and empty history to manage context efficiently. It provides the `delegate` method, which constructs the child's system prompt by combining a base instruction set with optional context, success criteria, and scratchpad support, then executes the child engine while wrapping standard output to draw a visual gutter that distinguishes the subagent's activity from the parent's. The module includes utility functions for managing the subagent's lifecycle, such as `child_settings` to pass configuration, `child_session_file` to generate unique log file names beside the parent's, and `fold_brief` to truncate long task descriptions in the parent's history. Finally, it defines the `answer` function, which processes the subagent's final state to extract a concise response for the parent, handling various termination conditions like context overflow, deadline expiration, tool round caps, or operator cancellation by appending appropriate status notes to the subagent's output.

### display.py

The `display.py` module defines a collection of terminal output events that function as leaves within the application's event graph, ensuring that visual feedback is rendered with high priority before subsequent processing steps occur. It establishes a base `DisplayEvent` class inheriting from the engine's `Event` and `Priority.HIGH`, from which several frozen dataclasses derive specific behaviors for printing messages to the console using a `Palette` and `c_out` utility without modifying the application state or emitting additional events. These display classes include `Info`, `Warn`, and `Error` for general messages with distinct color coding, `DisplayHistory` for rendering past conversation turns with specific formatting for user and assistant lines, `DisplayStats` for reporting context window usage and session token counts, and `DisplayBanner` for presenting a comprehensive startup summary of server settings, model parameters, tool configurations, and system prompts.

### events.py

The file `events.py` implements the core chat loop logic for a terminal-based AI assistant, structured as a collection of event classes that form a strongly connected component within the application's event graph. It manages the lifecycle of user interactions, ranging from prompting for input and handling command-line instructions to streaming model completions and executing tool calls with operator confirmation. The module defines the state transitions for a "turn," including budgeting token limits, building API requests with system prompts and history views, and implementing a compaction ladder that summarizes conversation history or pending turn rounds when the context window approaches its limit. It also handles edge cases such as detecting repeated model requests, managing scratchpad persistence, and processing user cancellations or denials of tool executions, ultimately freezing completed turns into the session history and persisting the state before returning control to the regeneration loop.

### gate.py

This file implements the operator-facing interface for tool call confirmation, handling how pending tool calls are displayed, how user decisions are captured, and how refusals are communicated back to the model. It defines an `Answer` dataclass to represent operator decisions—including yes, no, cancel, or auto-mode—and provides an `ask` function that reads single-key input from the terminal using `termios` and `tty` to capture choices like "y," "n," "m," "c," or "a" without requiring an Enter press, while also supporting a follow-up text prompt for providing specific guidance when declining. The module includes helper functions `shorten` to truncate and indent tool result text for display, and `describe_call` to format a `ToolCall` for the operator, preferring a custom preview from the `Tool` object if available, otherwise rendering JSON arguments as indented key-value pairs with specific palette colors for keys, values, and reasons. The file is designed to be called by the `ExecuteToolCalls` event handler, ensuring no callbacks are made back into the event system, and it exposes constants `DENIED_TEXT` and `SKIPPED_TEXT` that define the standard messages sent to the model when a tool call is refused or skipped due to a prior refusal in the same round.

### handlers.py

This module defines event handlers for the `desh_chat` application, specifically managing error recovery and user interruption logic within the event-driven engine. It introduces an `AutoOff` event class that modifies the `ChatState` to disable auto mode, addressing a limitation in the engine's interrupt mechanism that prevents state changes during the initial interrupt callback. The file provides an `on_error` function that prints the traceback and returns either a `TurnEnd` event to cancel an in-progress turn or a `MaybeRegenerate` event to return to the prompt, depending on whether a user turn is currently pending. Additionally, it implements an `on_interrupt` function that distinguishes between auto and manual modes; in auto mode, it emits events to disable auto mode, display an informational message, and either cancel the current turn or regenerate the prompt, while in manual mode, it displays an interruption message and exits the application.

### scratchpad.py

This file defines the data structures and utility functions for a scratchpad mechanism that serves as persistent working memory for an AI model across tool-calling rounds. It establishes a set of entry kinds—such as todo, done, fact, hypothesis, and block—to categorize information, along with a system prompt template that explains when tool results leave the context (the round cap, a checkpoint) and the necessity of persisting critical data. The module provides both mutable dictionary-based functions for immediate tool interactions and immutable dataclass-based classes, `Entry` and `Scratchpad`, to represent the state of this memory within the application's `ChatState`. Key operations include writing, deleting, and clearing entries, as well as a folding mechanism that replaces large values in echoed tool calls with size indicators to prevent context window overflow. Additionally, the `Scratchpad` class includes methods to serialize its state to a dictionary for storage or tool use and to render a formatted context string that groups entries by kind, displays the current round number relative to the turn cap, and says when the cap is one round away or reached.

### session.py

This file implements session persistence logic for a chat application by defining `LoadSession` and `SaveSession` events that manage the restoration and writing of conversation history to a specified JSON file. The `LoadSession` event, marked with high priority to ensure execution before user prompts, handles missing files by starting a new session and corrupt files by moving them to a `.bad` extension while initializing an empty state; it also restores the scratchpad and settings by replaying configuration changes on top of an initial seed found in the file. The `SaveSession` event performs atomic writes using a temporary file and `os.replace` to prevent data loss during crashes, preserving the original settings seed from the file if it exists while updating the history and adding metadata such as the save time, model, and system prompt. A helper function named `persist` is provided to generate the appropriate save event list after history-changing steps, ensuring the session file remains synchronized with the current state whenever a session file path is configured.

### state.py

This file defines the core state management and data structures for a chat-based AI agent system, establishing immutable dataclasses for `ChatState`, `ChatHistory`, `Turn`, `Round`, and `PendingTurn` to track conversation context, tool interactions, and session settings. It implements a sophisticated context window management strategy through token estimation and transcript fitting, utilizing the `fit_transcript` function to progressively reduce or omit older conversation segments to fit within a specified token budget. The module handles the lifecycle of tool calls and results, rendering a finished turn's tool outputs as expired stubs while preserving them for historical records, and it defines specific prompts for generating compact summaries of previous interactions to maintain conversational continuity. Additionally, it provides serialization and deserialization methods for persisting session state, including settings changes and scratchpad data, ensuring compatibility across different session file formats while managing the distinction between live pending turns and finalized history.

### toolset.py

The file `toolset.py` defines the default set of tools provided by the chat-des system, currently consisting of a single utility function, `current_time`, which returns the current date and time as an ISO 8601 string with an optional timezone offset parameter. It also includes a factory function, `default_registry`, which instantiates a `ToolRegistry` and registers the `current_time` function with `confirm=False`, indicating that this tool is read-only and does not require explicit operator confirmation before execution. The module's docstring explains that tools are plain functions whose schemas are derived by `Tool.define` from their signatures and Google-style docstrings, ensuring consistency between the model's understanding and the function's actual parameters.

