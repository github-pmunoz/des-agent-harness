# INDEX

_Auto-generated by `generate_index.sh`. Each section follows the workspace directory tree; each entry is a model-written description of the file's contents and how it is used._


## src/desh

### __init__.py

The file `__init__.py` is empty, serving solely to designate its parent directory as a Python package without defining any modules, classes, or initialization logic.

### engine.py

This file defines an abstract discrete-event simulation (DES) engine framework, providing the foundational classes for building simulations that process events in a specific order based on priority. It establishes a `State` abstract base class to represent simulation data and an `Event` abstract base class that requires subclasses to implement an `execute` method, which returns a new state and a list of subsequent events. The core `Engine` class manages a priority queue of events and provides three primary execution methods: `run` for a single final state result, `iter` for yielding the state after each step, and `trace` for collecting all intermediate states into a tuple. The engine enforces transactional guarantees by ensuring that state updates only occur if an event executes successfully, while also offering optional handlers for `KeyboardInterrupt` and general exceptions to allow for recovery or graceful shutdown. Additionally, the module includes support for structured JSONL logging and human-readable debug output to stderr, along with custom exception types like `SimulationError` and `HandlerError` to distinguish between contract violations and handler failures, and defines enums for tracking step outcomes and simulation exit modes.

### render.py

The file defines a `Palette` class that maps named channels—such as chrome, error, warning, user and assistant prompts, history summaries, statistics, diff lines, tool names and arguments, and Python syntax elements—to ANSI escape sequences for terminal coloring. It also sets up a bounded `reprlib.Repr` instance for truncating long representations and initializes two `Palette` instances, `c_out` and `c_err`, which enable coloring only when the respective standard output or error streams are connected to a terminal. A `Gutter` class wraps an output stream to prepend a configurable prefix to the start of every line, tracking line boundaries across arbitrary write chunks and preserving SGR color state so that multi-line colored content remains correctly styled; it delegates all non-write attributes to the underlying stream. Finally, the module provides two helper functions: `rl_prompt`, which wraps text in readline-compatible color escape sequences, and `pretty_log`, which formats a log record dictionary into a human-readable string displaying the payload, emitted fields, depth, duration, and any non-ok outcome.

### tools.py

This file implements a tool registry system that bridges the gap between Python callables and the JSON schemas required for model interactions. It defines a `Tool` dataclass that pairs a callable function with its OpenAI-shaped schema, confirmation policy, and metadata for previewing, injecting harness-specific parameters, and identifying calls for repetition guards. The module includes utility functions for deriving JSON schemas from Python type hints and parsing Google-style docstrings to extract parameter descriptions, ensuring that the schema accurately reflects the function signature. A frozen `ToolRegistry` dataclass manages a collection of tools, providing methods to register new tools, retrieve their schemas for request payloads, and invoke them with strict error handling that converts any exceptions into readable text for the model rather than propagating them, while also bounding output length to prevent context window overflow.


## src/desh/llama

### __init__.py

The file is an empty `__init__.py` that serves solely to mark its containing directory as a Python package, enabling the directory to be imported as a module namespace without defining any additional logic or attributes.

### esc_watcher.py

The `ESCWatcher` class in this file provides a mechanism to detect ESC keypresses on a POSIX terminal in order to interrupt LLM processes, explicitly noting that it is incompatible with Windows due to its reliance on `termios`. It operates by spawning a daemon thread that uses `select` to monitor standard input, distinguishing between a standalone ESC key and an escape sequence (such as arrow keys) by checking for additional bytes within a short grace period; if the grace period expires without further input, the `interrupted` flag is set to signal the main application. The class supports both manual `start` and `stop` calls and a context manager protocol, ensuring that terminal settings are restored to their original state upon cleanup.

### logger.py

This file defines a `Logger` class responsible for persisting completion data in a JSON Lines (JSONL) format, ensuring compatibility with existing shell script queries by mirroring the record structure of `send_direct.sh`. The class is initialized with a file path that is expanded and stored for subsequent use. Its primary method, `record`, constructs a dictionary containing an ISO-formatted timestamp, the server port, the request payload, and the serialized completion response. Before writing, the method ensures the target directory exists and then appends the JSON-serialized record to the specified file, intentionally omitting error states to maintain the design principle that each log entry represents a single successful completion.

### pyscan.py

This file implements a stateful, streaming syntax highlighter for Python code blocks, designed to process text fragments as they arrive rather than requiring the complete source at once. It defines a `ScanState` dataclass to track whether the scanner is inside a string or comment and to hold back ambiguous tokens, along with a `scan` function that classifies incoming text into colored spans based on specific channels like keywords, builtins, calls, strings, numbers, and comments. The logic enforces a "decisive" rule where tokens are only emitted once their classification cannot change, holding back incomplete identifiers, partial string delimiters, or escaped characters until the next fragment confirms their type. A `classify` helper determines the channel for identifiers by checking against standard library keywords and a curated list of builtins, while a `finish` function allows the scanner to flush any remaining pending state at the end of a code fence. The module also includes private utilities for advancing through character runs, resolving string opening delimiters (handling single vs. triple quotes), and scanning within string bodies to detect closing delimiters and escape sequences.

### send_direct.py

This file implements a Python command-line interface for sending single requests to a local `llama-server` instance, serving as a port of an earlier bash prototype. It defines a helper function to format file contents into a numbered, tag-delimited prompt block and a main entry point that uses `argparse` to handle various flags for configuring the request, such as temperature, max tokens, system prompts, and streaming behavior. The script constructs a `Request` object from the provided arguments, optionally printing the payload without sending it in no-op mode. When active, it instantiates a `LlamaServer` client and either streams the response through a rendering chain of `Seam`, `CodeFence`, `PyHighlight`, and `Terminal` objects for live, colorized output, or completes the request to retrieve the final content. It handles connection and server errors by printing messages to standard error and returning a non-zero exit code, while also supporting the optional logging of telemetry to a JSONL file and writing the resulting content or full JSON completion to a specified output file or standard output.

### server.py

This file defines the `LlamaServer` class, which serves as the HTTP transport layer for communicating with a single `llama-server` instance, supporting both single-model and router configurations. It establishes a specific error taxonomy using `LlamaUnreachable` for connection failures or timeouts and `LlamaServerError` for structured error responses received from the server, either as HTTP error bodies or as mid-stream frames. The module provides methods for non-streaming completions via `complete()`, raw streaming via `stream_raw()`, and a "T-junction" streaming method `stream()` that simultaneously accumulates frames for final completion construction and dispatches channel-specific text events to a `Renderer` object, which is defined by a `Renderer` protocol requiring `feed` and `flush` methods. Additional utility methods include `models()` and `props()` for querying server status and configuration, and `max_context()` for extracting context size limits from model launch arguments, all of which rely on a private `_open` helper that handles JSON serialization, HTTP request execution, and the mapping of low-level `urllib` exceptions to the module's custom error types.

### stages.py

This file implements a chain of renderer stages used to process and display streaming completion events, structured as a linked list where each `Stage` transforms incoming data and passes it to the next link in the sequence. The module defines several specific stage classes, including `ToolProgress` which visualizes streaming tool calls by rewriting a single line with character counts, `PyHighlight` which uses a streaming scanner to colorize Python code tokens, `Terminal` which serves as the final sink that writes colored text to a stream, `Seam` which inserts a separator between reasoning and content sections, and `CodeFence` which parses markdown code fences to re-channel content into distinct code streams. The stages are designed to be composed together, with the outermost stage receiving raw channel events and the innermost stage handling the final output, allowing for complex formatting and highlighting of different types of streamed data.

### tokens.py

The file `tokens.py` implements token accounting logic for tracking resource usage during AI model interactions, defining two distinct regimes for calculating token counts: a pre-completion estimation phase and a post-completion verification phase. It provides an `estimate_tokens` function that approximates the token count of a string by dividing its character length by four, serving as a heuristic for scenarios where the actual tokenizer is not yet available or practical to invoke. Additionally, the file includes a `turn_tokens` function that calculates the precise token cost for a specific user-assistant exchange to be used in history budgeting, relying on a server-provided usage dictionary when available to determine prompt and completion tokens. When usage data is missing or yields non-positive values, the function falls back to the character-based heuristic to ensure that any non-empty turn is assigned a positive token count, preventing downstream logic from treating the turn as unpriced.

### wire.py

The `wire.py` module defines the data structures and parsing utilities for interacting with the llama-server chat completion API, specifically modeling the request payload and the resulting completion object. It provides a `Request` dataclass that constructs the JSON body for the `/v1/chat/completions` endpoint, handling optional parameters like model selection, tool schemas, and streaming options. The module also includes a `ToolCall` dataclass to represent individual tool invocations within a response and a `Completion` dataclass that encapsulates the final settled state of a chat interaction, including content, reasoning, usage statistics, and tool calls. The `Completion` class offers two factory methods: `from_response` for non-streaming JSON responses and `from_frames` for folding a sequence of Server-Sent Events (SSE) frames into a single completion object, correctly accumulating fragmented tool call arguments and separating reasoning from content. Additionally, the file exposes two pure helper functions, `parse_sse` and `events`; the former parses raw SSE data lines into JSON frames while ignoring the `[DONE]` sentinel, and the latter translates individual frames into channel-specific text events (such as "reasoning," "content," "tool_name," and "tool_args") for use by a rendering layer, ensuring that the frame structure is interpreted in a single, centralized location.


## src/desh_chat

### __init__.py

The file is an empty Python module marker that serves to designate its containing directory as a package, allowing it to be imported by other parts of the project without containing any executable code or definitions.

### cli.py

This file serves as the command-line entry point for a discrete-event simulation chatbot, utilizing the `argparse` module to parse a comprehensive set of configuration flags that define inference parameters, logging destinations, workspace boundaries, and specific toolset permissions. It includes a `build_tools` function that constructs a `ToolRegistry` by conditionally adding file manipulation, bash execution, time retrieval, and a delegate subagent capability based on the provided flags, ensuring that delegated subagents inherit the parent's resolved session and logging contexts. A helper function named `resolve_session_file` manages the persistence logic by determining the correct file path based on explicit session arguments or a designated folder for auto-naming, creating necessary directories as needed. The `main` function orchestrates the initialization of the application by establishing a connection to a local Llama server, defining a `ChatState` object that encapsulates the inference engine, history, settings, and tool registry, and printing a formatted header that summarizes the active configuration. Finally, it instantiates and runs the `Engine` class with the prepared state, passing in specific event handlers for session loading and regeneration, along with error and interrupt callbacks, to begin the interactive chat session.

### coding.py

This file defines a sandboxed toolset for a coding agent, centered on a frozen `Workspace` dataclass that binds all operations to a single project root to prevent path traversal and symlink escapes. The `Workspace` class exposes four primary methods—`read`, `write`, `edit`, and `bash`—which serve as the interface for the agent to interact with the filesystem and execute commands, with `read` operating without user confirmation while the other three require operator approval. The `bash` method enforces a mandatory `reason` parameter to articulate intent before executing a command, and a helper function `edit_preview` generates a color-coded unified diff to display proposed changes to the user during the confirmation process. Finally, the `coding_registry` function instantiates a `Workspace` and returns a configured `ToolRegistry` that registers these methods with specific names, confirmation settings, and identity definitions to manage how the agent’s calls are validated and presented.

### commands.py

This file defines the command interface for the desh_chat application, establishing a central registry that both the completion system and the dispatch logic reference to ensure consistency. It introduces a frozen `CommandSpec` dataclass to store command metadata and a `Command` dataclass that inherits from an `Event` base class, serving as the executable unit for user-initiated actions. The `Command` class implements an `execute` method that dispatches to specific handler methods based on the command name, while catching `CommandError` exceptions to provide user-friendly warnings without crashing the application. The file includes a suite of private handler methods for various chat settings and actions, such as adjusting context window size, changing the model, setting temperature, and toggling auto or thinking modes, each of which returns updated state and a list of events to trigger subsequent UI or logic updates. Argument parsing utilities are provided to validate inputs, ensuring that commands receive the correct number and type of arguments before execution. Finally, the module exports a `COMMANDS` dictionary that maps canonical command names to their specifications, which serves as the single source of truth for the application's command surface, and constructs a `_COMMAND_INDEX` that resolves both canonical names and aliases to their corresponding command specifications for efficient dispatch.

### delegate.py

This module implements the `Delegate` tool, which allows a primary AI agent to offload specific subtasks to a subagent to manage context window limits by isolating intermediate tool interactions and reads from the parent's conversation history. The file defines the `Delegate` dataclass, which holds configuration such as the inference engine, tool registry, and session file paths, and exposes a `delegate` method that constructs a new `ChatState` for the subagent with a specialized system prompt, runs it synchronously via the `Engine` class while redirecting standard output to render a visual gutter, and returns the subagent's final answer or a status message. It includes helper functions to derive child settings, generate unique session file names for subagent logs, and fold long task briefs for display, along with a logic block that executes an optional shell command check after the subagent finishes to append verification results to the response. The module also defines the system prompt for the subagent, which instructs it to orient itself within the project, use tools efficiently, and provide a concise final answer, as well as an `answer` function that interprets the subagent's final state to determine whether the task succeeded, was cancelled, hit a resource limit, or failed to fit within the context window.

### display.py

The `display.py` file defines a set of leaf-node event classes within the `desh_chat` project that serve as terminal sinks for printing formatted output to the console without emitting subsequent events. It establishes a base `DisplayEvent` class inheriting from the engine's `Event` type with high priority to ensure display messages appear before the next processing step, followed by specific frozen dataclass implementations for `Info`, `Warn`, and `Error` messages that print colored text using a shared palette. The module also includes `DisplayHistory` and `DisplayStats` events, which retrieve data from the `ChatState` object to print conversation transcripts with distinct colors for user and assistant turns, and to calculate and display current context and session token usage against the configured context limit, respectively.

### events.py

This file implements the core chat loop of an interactive AI assistant application, structuring the interaction as a graph of immutable `Event` dataclasses that transform a shared `ChatState`. The module defines the lifecycle of a conversation turn, beginning with `MaybeRegenerate` and `TurnStart` to manage the session state and determine whether to prompt the user, continue an automated task, or exit. It handles user input through `PromptUser`, which distinguishes between standard messages and slash-commands, and processes those commands via a deferred import to avoid circular dependencies. The turn execution is orchestrated by `NextRound`, which calculates token budgets, builds the LLM request, and decides when to invoke `CompactHistory` to summarize older context if the window becomes too full. Once a request is ready, `StreamCompletion` manages the streaming of the model's response, monitoring for cancellation via an `ESCWatcher` and routing the outcome to either `AppendRound` if tool calls are requested, or `TurnEnd` if the response is complete. Tool execution is handled sequentially by `ExecuteToolCalls`, which processes each call one at a time, handling operator confirmations, denials, and automatic mode toggles before attaching results to the pending turn. The module concludes with `TurnEnd`, which finalizes the current turn into the persistent history, saves the session, and returns control to the start of the loop, while also including utility functions for formatting tool arguments and managing readline completion for commands.

### gate.py

This file implements the operator-facing interface for tool call confirmations, providing functions to display pending calls and capture user decisions. It defines an `Answer` dataclass to represent operator choices such as yes, no, cancel, or auto-mode, optionally including a custom message to guide the model when a call is declined. The `ask` function handles interactive input by reading single keystrokes from the terminal in cbreak mode, allowing the operator to approve, deny, cancel, or provide context for a specific `ToolCall` without requiring a full line entry. Supporting utilities include `shorten`, which formats tool result text for concise display with indentation and character limits, and `describe_call`, which renders a `ToolCall` for the operator by parsing JSON arguments, applying custom tool previews when available, and formatting multi-line values into indented blocks with color-coded keys and values. The module is designed to be called by the `ExecuteToolCalls` event handler and relies on external rendering utilities for terminal output and palette management.

### handlers.py

This file defines event handlers for the `desh_chat` application, specifically managing state transitions related to error handling and user interruption. It introduces an `AutoOff` event class that inherits from the engine's `Event` base class and, upon execution, updates the current `ChatState` to disable the auto mode setting. The module also provides two callback functions, `on_error` and `on_interrupt`, which are likely registered with the application's event engine. `on_error` prints a traceback for debugging and returns a `MaybeRegenerate` event to trigger a retry or refresh. The `on_interrupt` function handles user cancellation (such as a Ctrl+C signal) by checking if auto mode is active; if so, it displays a status message, emits the `AutoOff` event to disable the mode, and returns `MaybeRegenerate` to return control to the prompt, whereas in normal mode it displays an interruption message and returns an `Exit` event to terminate the application.

### session.py

This file implements session persistence for a chat application by defining two high-priority event classes, `LoadSession` and `SaveSession`, which manage the reading and writing of conversation history to a file specified in the application's state. The `LoadSession` event restores previous chat turns from a JSON file, handling missing files by starting a new session and corrupt files by moving them to a backup location with a `.bad` extension, while also populating the command-line history with previous user inputs. The `SaveSession` event writes the entire current history to the session file using an atomic operation that involves writing to a temporary file and then replacing the original, ensuring data integrity during crashes and including metadata such as save time, model, context, and system prompt in the JSON output. A helper function named `persist` is provided to generate a `SaveSession` event whenever a history-changing step occurs, ensuring that the session file is updated immediately after any modifications to the chat history.

### state.py

This file defines the core immutable data structures and state management logic for a chat application, utilizing frozen dataclasses to ensure state consistency. It introduces `Settings` to configure model parameters, token limits, and compaction thresholds, along with a default `COMPACTION_PROMPT` designed to summarize working sessions by retaining critical technical details while discarding superseded context. The `ChatState` class serves as the central immutable state container, holding references to the inference engine, tool registry, session file, and current chat history, while providing helper methods to calculate token budgets, generation room, and compaction triggers. The file details the internal mechanics of a single conversational turn through the `PendingTurn` and `Round` classes, which track user messages, intermediate model responses, tool calls, and tool results, including methods for serializing these exchanges into wire-format messages and estimating token costs. Finally, it defines the `Turn` and `ChatHistory` classes to manage the persistent record of completed interactions, offering utilities for viewing recent history within a token budget, calculating total session tokens, and compacting the history by replacing older turns with a synthetic summary turn to manage context window limits.

### toolset.py

This file defines the default set of tools provided by the chat-des application, structured so that each tool is a plain function whose schema is automatically derived from its signature and Google-style docstring by the `Tool.define` mechanism. It includes a `current_time` function that returns the current date and time as an ISO 8601 string, with an optional parameter to adjust the local clock by a specified number of hours. The file also contains a `default_registry` function that instantiates and returns a `ToolRegistry` object with the `current_time` tool registered, explicitly setting `confirm=False` to declare the tool as read-only and exempt it from the standard operator confirmation requirement before execution.

