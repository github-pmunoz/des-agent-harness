# INDEX

_Auto-generated by `generate_index.sh`. Each section follows the workspace directory tree; each entry is a model-written description of the file's contents and how it is used._


## src/desh

### __init__.py

The file is an empty Python module initialization file, typically used to mark the directory as a package without defining any specific contents or logic.

### engine.py

The `engine.py` file implements an abstract discrete-event-simulation framework, defining the core components required to manage simulation state, event processing, and execution control. It establishes `State` as an empty abstract base class for simulation data and `Event` as a generic abstract base class that carries a priority level and defines an abstract `execute` method responsible for transforming the current state and generating a list of new events. The central `Engine` class utilizes a heap-based priority queue to process events sequentially, ensuring that each step is executed atomically so that the simulation state is only updated if the step completes successfully, thereby providing a transactional guarantee where failed steps leave the state unchanged. The engine supports three primary execution modes: `run()`, which processes all events and returns the final state; `iter()`, which yields the state after each step for streaming consumption; and `trace()`, which collects and returns a tuple of all intermediate states. Robust error handling is integrated through optional `on_error` and `on_interrupt` callbacks that can recover from exceptions or keyboard interrupts by generating replacement events, while unhandled errors or handler failures trigger specific exit modes and raise custom exceptions like `SimulationError` or `HandlerError`. Additionally, the engine provides optional JSONL logging and human-readable debug output to stderr, recording detailed metadata for each step such as event names, payload representations, execution duration, queue depth, and the specific outcome of the step to facilitate post-run analysis and debugging.

### render.py

The file defines a color palette system and formatting utilities for terminal output, creating two `Palette` instances—one for standard output and one for error output—that conditionally apply ANSI escape codes based on whether the respective stream is connected to a TTY. It configures a `reprlib.Repr` instance with specific length limits for strings and other objects, establishes a `Palette` class that wraps text with color codes and a reset sequence when enabled, and provides helper functions: `rl_prompt` to format prompt text with readline-specific escape sequences for colorized display, and `pretty_log` to format a log record dictionary into a human-readable string that includes the payload, emission list, depth, duration, and an optional warning indicator for non-successful outcomes.

### tools.py

This file defines the tooling infrastructure for the engine package, establishing the registry system that determines which capabilities are exposed to the model via JSON schemas and how those capabilities are executed as Python callables. It implements a strict schema derivation process that converts Python function signatures, type hints, and Google-style docstrings into OpenAI-compatible JSON schemas, ensuring that the model’s view of a tool’s parameters matches the function’s actual acceptance criteria to prevent argument mismatches. The `Tool` dataclass encapsulates a single callable along with its derived schema and a confirmation policy that defaults to requiring operator approval before execution, while the frozen `ToolRegistry` class manages a collection of tools, providing methods to register new tools, retrieve their schemas for API requests, and invoke them by name. The invocation logic is designed to be fault-tolerant from the model’s perspective, catching JSON decoding errors, argument binding failures, and runtime exceptions to return descriptive text results rather than propagating errors, and it includes a bounding mechanism that truncates excessively large tool outputs to protect the context window.


## src/desh/llama

### __init__.py

The file is an empty `__init__.py` marker that designates its directory as a Python package, enabling the directory to be imported as a module by the Python interpreter.

### esc_watcher.py

The `ESCWatcher` class in `esc_watcher.py` provides a mechanism to detect ESC keypresses from standard input within a separate daemon thread, specifically designed to interrupt LLM operations on POSIX systems. It operates by temporarily switching the terminal to cbreak mode to allow non-blocking character reads, using the `select` module to poll for input with a short timeout. The watcher distinguishes between a bare ESC keypress and the start of an escape sequence (such as arrow keys) by checking for additional bytes within a defined grace period; if no further input arrives, it sets an `interrupted` flag to signal the main application. The class supports both explicit start and stop methods and a context manager protocol, ensuring that terminal settings are saved upon initialization and restored upon exit, while the stop method joins the monitoring thread and reverts the terminal state.

### logger.py

The `Logger` class in this file is responsible for persisting completed requests to a JSON Lines file, ensuring that the data layout matches the format generated by an external `send_direct.sh` script to maintain compatibility with existing query tools like `jq`. It accepts a file path during initialization and provides a `record` method that takes a `Request` object, a `Completion` object, and a port number as arguments. When invoked, the method constructs a dictionary containing the current timestamp, the port, the request payload, and the completion response, then creates the necessary parent directories and appends the serialized JSON record to the specified file. This design intentionally excludes error states from the log, adhering to a policy where each file entry represents a single successful completion.

### send_direct.py

This file serves as a command-line interface for interacting with a llama-server instance, acting as a Python port of a previous bash prototype. It defines a main function that parses a variety of command-line arguments to control request parameters such as temperature, token limits, and system prompts, as well as output behaviors like streaming, logging, and file redirection. The script constructs a single request object, optionally populating it with numbered content from a specified file, and then dispatches this request to a local LlamaServer instance. Depending on the configuration, it handles the response either by streaming the output through a rendering chain that applies specific formatting to reasoning and code blocks, or by processing the complete response and printing or writing the final text and metadata to the specified destination.

### server.py

This module defines the `LlamaServer` class, which serves as the HTTP transport layer for communicating with a single `llama-server` instance, supporting both single-model and router configurations. It exposes a protocol for `Renderer` objects that can consume streamed events and establishes a specific error taxonomy with `LlamaUnreachable` for connection failures and `LlamaServerError` for structured error responses returned by the server. The class provides methods for non-streaming completions via `complete()`, raw streaming via `stream_raw()`, and a high-level `stream()` method that acts as a "T-junction," simultaneously accumulating frames for a final `Completion` object and feeding parsed events to a provided renderer. Additionally, it includes utility methods to query available models, retrieve server properties for specific models, and determine the maximum context size for loaded models by inspecting their launch arguments.

### stages.py

The `stages.py` file implements a chain of responsibility pattern for rendering streaming AI completions, defining a base `Stage` class that forwards events to subsequent links in the pipeline. It includes specific transformation stages: `ToolProgress` converts tool call events into a dynamic progress indicator that displays the tool name and argument size without revealing the actual content; `Terminal` acts as the final sink, writing output to a stream with optional ANSI color coding based on the event channel; `Seam` inserts a visual separator between reasoning and content blocks; `CodeFence` reclassifies text within triple-backtick delimiters from general content to code (a python fence body as `code_py`), carefully handling cases where fence markers and the info string are split across streaming chunks; and `PyHighlight` colours a python body by token class through the streaming scanner in `pyscan.py`, emitting `py_kw`/`py_builtin`/`py_call`/`py_str`/`py_num`/`py_comment` channels that only `Terminal` maps to colours. These stages are designed to be chained together, with the outermost stage receiving the initial events from the language server and passing transformed events downstream until they reach the terminal output.

### pyscan.py

The `pyscan.py` file is the streaming python scanner behind `PyHighlight`: a pure function `scan(state, text)` over a frozen `ScanState` that classifies a python fence body into `(channel, text)` spans while the text is still arriving as arbitrary fragments. A token is emitted only once it is decisive; three tokens can run off the end of a fragment and are held back for the next call — an identifier or number run, a run of fewer than three quotes (a triple quote may be forming), and a backslash inside a string. `classify(word, after)` decides an identifier's channel with identity beating use: keywords, then the curated builtin set, then a name followed by `(` as a call. `finish(state)` classifies whatever is pending and resets, so each fence starts as a fresh program. Not a tokenizer: strings, numbers, comments and identifiers are the whole scope.

### tokens.py

The file `tokens.py` implements token accounting logic for managing context window limits and pricing conversation turns, distinguishing between a pre-completion estimation regime and a post-completion measurement regime. It provides `estimate_tokens`, a simple heuristic function that approximates token count by dividing character length by four, intended for use before a model completion occurs when precise tokenization is unavailable. The primary function, `turn_tokens`, calculates the token cost of a completed interaction turn for history budgeting by combining the server-reported prompt and completion usage data with the specific text of the user and assistant messages. This function adjusts the reported token counts to isolate the contribution of the current user message and the re-sent portion of the assistant's reply, subtracting prior context tokens and proportionally allocating completion tokens between visible content and reasoning, while falling back to character-based estimates if usage data is missing or the calculated values are non-positive.

### wire.py

The file `wire.py` defines the data structures and pure helper functions for handling the wire protocol of llama-server chat completions within the desh project. It introduces three dataclasses: `Request`, which constructs the payload for the `/v1/chat/completions` endpoint by conditionally including optional keys like `model`, `tools`, and `seed` based on their presence; `ToolCall`, a frozen record representing a single entry in a completion's `tool_calls` array; and `Completion`, which represents a settled chat completion and can be constructed either directly from a non-streaming JSON response or by folding a sequence of Server-Sent Events (SSE) frames. The `Completion` class handles the accumulation of content, reasoning, and tool-call arguments across multiple streaming frames, ensuring that tool-call identifiers and names are captured once while arguments are concatenated. Additionally, the module provides two pure utility functions: `parse_sse`, which iterates over lines to extract and parse JSON frames while filtering out blank lines and the `[DONE]` sentinel, and `events`, which translates a single frame into a stream of `(channel, text)` tuples for rendering, distinguishing between reasoning, content, tool names, and tool argument fragments. The module is designed to be self-contained, performing no I/O and importing only standard library modules, serving as the sole place where the specific shape of llama-server's SSE frames is interpreted for the rest of the application.


## src/desh_chat

### __init__.py

The file is an empty `__init__.py` marker that serves solely to designate its containing directory as a Python package, enabling the import machinery to recognize the folder structure without defining any specific classes, functions, or configuration data.

### cli.py

This file serves as the primary command-line entry point for a discrete-event-simulation chatbot, defining the `main` function that parses extensive command-line arguments for configuration parameters such as server port, model identifier, temperature, context window size, and toolset selection. It includes a helper function, `resolve_session_file`, that determines the appropriate file path for persisting chat session data based on whether an explicit session file or a sessions folder is provided, creating necessary directories as needed. The script initializes a `LlamaServer` client and constructs a `ChatState` object that aggregates settings, inference engine details, chat history, and a tool registry selected from predefined options like "none," "basic," or "coding," which determines the available tools and default system prompt. After printing a formatted configuration summary to the console, the script instantiates and runs a generic `Engine` class parameterized by `ChatState`, passing in error and interrupt handlers and seeding the simulation loop with initial events to load a session and prompt the user.

### coding.py

This module defines the `Workspace` class and a `coding_registry` factory that provide a constrained set of file and shell operations for a coding agent, ensuring all actions remain within a specified project root by validating paths and rejecting symlinks. It exposes four primary tools—Read, Write, Edit, and Bash—as methods of the `Workspace` dataclass, where Read operates without user confirmation while Write, Edit, and Bash require operator approval, with the latter demanding a plain-language reason before executing a shell command. The file also includes a helper function, `edit_preview`, which generates a colorized unified diff for Edit operations to aid operator review, and a system prompt string that instructs the agent to use the tools responsibly and adapt to rejections rather than retrying failed actions.

### commands.py

This file defines the command interface for a chat application, establishing a central registry and event structure for processing user inputs. It introduces a `Command` event class that extends a base `Event` type, containing logic to dispatch commands to specific handler methods based on a canonical command name or alias. The module defines several handler methods for actions such as compacting history, adjusting context windows, exiting the application, displaying history, setting token limits, listing or selecting models, configuring temperature, and toggling thinking modes, each returning updated state and a list of resulting events. To support these handlers, it includes utility methods for parsing arguments into integers or floats with range validation, raising a custom `CommandError` exception for user-facing issues that are caught during execution to produce warning events. A `COMMANDS` dictionary serves as the single source of truth for available commands, mapping canonical names to `CommandSpec` objects that hold descriptions and handler references, while a derived `_COMMAND_INDEX` dictionary expands this registry to include aliases for efficient dispatch. The module's design ensures that completion, dispatch, and help listings all read from this unified registry, while maintaining a specific import pattern with `events.py` to avoid circular dependencies.

### display.py

This file defines a collection of display event classes that serve as terminal sinks in the application's event graph, prioritized to ensure output appears before subsequent processing steps execute. Each class inherits from a base `DisplayEvent` and implements an `execute` method that prints specific information to the console using a color palette while returning the unchanged state and an empty list of new events. The `Info`, `Warn`, and `Error` classes handle standard status messages with distinct color codes, while `DisplayHistory` iterates through the chat state's history to print either turn summaries or full transcripts with user and assistant lines differentiated by color. Additionally, the `DisplayStats` class calculates and prints the current context window usage and total session token counts by estimating the system prompt tokens and summing historical token usage.

### events.py

This file implements the core chat loop for the application, structuring the interaction between the user and the AI model as a strongly connected component of an event graph. It defines several frozen dataclass events that drive the state machine, including `MaybeRegenerate` and `PromptUser` which handle user input and command parsing, and `UserMessage` which initiates a new turn by setting up a pending state. The turn logic is managed through a sequence of events: `NextRound` calculates token budgets and constructs the API request, `StreamCompletion` handles the streaming of the model's response and routes control based on the finish reason, and `AppendRound` and `ExecuteToolCalls` manage multi-step tool interactions, including operator confirmation gates and handling of denials or cancellations. The `TurnEnd` event finalizes a turn by moving the pending state into the history, triggering potential history compaction via `MaybeCompact` and `CompactHistory`, which summarizes older conversation segments to fit within context limits. The module also configures readline completion for command auto-suggestion and includes a `LogCompletion` event for recording inference requests and responses.

### gate.py

The `gate.py` module implements the operator interface for confirming tool executions, serving as the human-in-the-loop component that sits between the model's intent and the actual tool invocation. It defines an `Answer` dataclass to capture the operator's decision—whether to approve, deny, or cancel a call—along with an optional message that provides context to the model when a tool is rejected. The primary `ask` function handles interactive terminal input by temporarily switching the terminal to raw mode to capture immediate keypresses for quick decisions like 'yes', 'no', 'cancel', or requesting a message, while also providing a fallback line-based prompt for entering detailed feedback; this function is designed to never raise exceptions and returns a cancel decision if input is unavailable or interrupted. To support the visual presentation of pending actions, the module includes a `describe_call` function that formats a `ToolCall` for display, utilizing a tool-specific preview method if available (such as rendering a diff for edit operations) or falling back to a structured list of arguments with multi-line values indented for readability, ensuring that the operator always has a clear view of what is about to be executed. Additionally, a `shorten` utility is provided to truncate long text for terminal echoes, replacing newlines with a visual indicator and limiting output length to maintain a clean interface. The module operates without emitting events, relying instead on being called by the `ExecuteToolCalls` logic in `events.py`, and its `ask` function is designed to be easily mocked in tests to script operator responses.

### handlers.py

The file `handlers.py` defines two event handler functions used within the `desh_chat` application to manage error and interrupt conditions. The `on_error` function processes exceptions by printing their stack traces to the console and returning a list containing a `MaybeRegenerate` event, while the `on_interrupt` function responds to user interrupts by returning a list of events that includes an informational message indicating the interruption and an `Exit` event to terminate the application.

### session.py

This file implements session persistence logic for a chat application by defining two high-priority event classes, `LoadSession` and `SaveSession`, which manage the restoration and atomic writing of chat history to a file specified in the application state. The `LoadSession` event initializes the session by reading JSON data from the designated path, restoring the conversation history and populating the readline interface with previous user inputs, while handling missing files as new sessions and corrupt files by moving them to a backup location and starting fresh with a warning. Conversely, `SaveSession` serializes the current history along with metadata such as the timestamp, model, context, and system prompt into a temporary file before atomically replacing the original session file to ensure data integrity during crashes. The module also provides a `persist` helper function that returns a list containing the `SaveSession` event when a session file is configured, allowing other parts of the application to easily trigger persistence after history modifications.

### state.py

This file defines the immutable data structures that constitute the conversational state for the DESH chat engine, organizing the application’s context into settings, inference configuration, and a persistent chat history. It establishes a `ChatState` dataclass that serves as the primary state container, holding references to user preferences, an `InferenceEngine` for model access, a `ToolRegistry` for available functions, and a `PendingTurn` object that tracks the in-progress interaction between a user message and the model’s final response. The file details the mechanics of tool usage through the `Round` and `ToolResult` classes, which encapsulate intermediate model outputs and tool executions, allowing the state to maintain a record of multi-step reasoning before a turn is finalized. For long-term persistence, the `ChatHistory` and `Turn` classes provide serialization capabilities, including versioned dictionary conversion to handle session files, while also implementing logic for context window management through methods that calculate token budgets, retrieve recent conversation tails, and generate synthetic summary turns to compact older history.

### toolset.py

This file defines the default set of tools provided by the chat-des application, specifically implementing a `current_time` function that returns the current date and time as an ISO 8601 string with an optional timezone offset. The module utilizes the `Tool.define` mechanism to derive schemas from function signatures and docstrings, ensuring alignment between the model's instructions and the function's actual parameters, while also establishing a confirmation policy where tools require operator approval unless explicitly marked as read-only. The `default_registry` function constructs and returns a `ToolRegistry` instance that registers the `current_time` tool with `confirm=False`, indicating that it is a safe, read-only operation that does not require user verification before execution.

