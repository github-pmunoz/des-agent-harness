# INDEX

_Auto-generated by `generate_index.sh`. Each section follows the workspace directory tree; each entry is a model-written description of the file's contents and how it is used._


## src/desh

### __init__.py

This file is an empty Python module marker that serves solely to designate its parent directory as a Python package, allowing it to be imported within the project structure.

### engine.py

This file implements an abstract discrete-event simulation (DES) engine that provides the core infrastructure for running simulations based on event queues and state transitions. It defines an abstract base class `State` for simulation states and a generic `Event` class that requires subclasses to implement an `execute` method, which takes a state and returns a new state along with a list of subsequent events to process. The `Engine` class orchestrates the simulation loop by maintaining a priority-based event queue, processing events atomically to ensure state consistency, and managing error handling through optional `on_error` and `on_interrupt` callbacks that can inject recovery events. The engine offers three primary entry points: `run` to execute the simulation to completion and return the final state, `iter` to yield the state after each step for streaming consumption, and `trace` to collect all intermediate states into a tuple. It also includes robust logging capabilities that can write detailed JSONL records to a file or print human-readable debug information to stderr, tracking metrics such as step duration, queue depth, and outcome status, while distinguishing between normal termination, unhandled exceptions, and handler failures via specific exit modes and custom exception types.

### render.py

The `render.py` file provides utility classes and functions for formatting terminal output with ANSI escape codes, primarily to support a CLI interface that displays chat interactions, tool executions, and code snippets. It begins by defining a bounded representation helper to truncate long strings, followed by a `Palette` class that encapsulates a comprehensive set of ANSI color codes for various UI elements such as user and assistant messages, error states, diff views, and Python syntax highlighting. This palette is instantiated for both standard output and standard error streams, automatically enabling colorization only when the output is connected to a TTY. The file also introduces a `Gutter` class, which acts as a wrapper for text streams to automatically prefix every new line with a specified indentation string, allowing nested execution contexts like subagents to be visually indented; this class carefully tracks line boundaries and current SGR color states to ensure prefixes are applied correctly even when content is streamed in chunks or when escape sequences are present. Finally, the module exposes two helper functions: `rl_prompt`, which formats text for use with the Readline library to preserve color codes, and `pretty_log`, which formats a dictionary of execution records into a human-readable string including payload, emitted items, depth, duration, and outcome status.

### tools.py

This file implements a tool registry system that bridges Python callables with OpenAI-compatible JSON schemas for model interaction, residing within the engine package to serve both transport and execution layers. It defines the `Tool` dataclass, which encapsulates a callable, its derived or hand-written schema, confirmation policies, and metadata for preview rendering, parameter injection, and call identity tracking, while the `ToolRegistry` dataclass manages an immutable collection of these tools and provides methods to generate request schemas, resolve tool identities for repetition guards, and safely invoke functions by parsing JSON arguments, injecting harness-provided parameters, and bounding result sizes. The module includes utility functions that derive JSON Schema fragments from Python type hints using strict rules that raise errors for unsupported annotations, and parses Google-style docstrings to extract descriptions and parameter documentation, ensuring that the function signature remains the single source of truth for the tool definitions presented to the model.


## src/desh/llama

### __init__.py

The file `__init__.py` is empty and serves solely to mark its directory as a Python package, enabling module imports within the project.

### esc_watcher.py

The `esc_watcher.py` file provides an `ESCWatcher` class designed to monitor for ESC keypresses in a background thread, primarily to allow users to interrupt long-running LLM operations. It relies on POSIX-specific modules like `termios` and `tty`, making it incompatible with Windows systems where these imports would fail. The class supports context manager usage, ensuring that the watcher starts when entering the block and stops when exiting, which involves saving and restoring the original terminal settings. Internally, the `start` method puts the standard input into cbreak mode if it is a TTY and launches a daemon thread that runs the `_watch` loop. This loop uses `select` to efficiently wait for input, distinguishing between a bare ESC keypress and multi-byte escape sequences (such as those from arrow keys) by checking for additional bytes within a short grace period; if no follow-up bytes are detected, it sets an `interrupted` flag to signal the main application. The `stop` method clears the interruption flag, joins the monitoring thread, and restores the saved terminal attributes to ensure the user's shell environment is left in a functional state.

### logger.py

The `logger.py` file defines a `Logger` class designed to persist completion data as JSON Lines records, ensuring compatibility with existing shell script queries by mirroring the output layout of `send_direct.sh`. The class constructor accepts a file path, which is expanded and stored for later use, while the `record` method constructs a dictionary containing the current timestamp, the target port, the request payload, and the serialized completion response. Before writing, the method ensures the parent directory exists and then appends the JSON-serialized record to the specified file, explicitly excluding error states from the log to maintain the invariant that each line represents a single successful completion.

### pyscan.py

The `pyscan.py` file implements a streaming Python syntax scanner for a renderer, designed to classify text fragments into colored spans as they arrive, without requiring the complete input. It defines a `ScanState` dataclass to track the current scanning context, including whether the scanner is inside a string or comment, and any pending text that has not yet been decisively classified. The core logic resides in the `scan` function, which processes incoming text by identifying keywords, builtins, function calls, strings, numbers, and comments, assigning each to specific channels like `py_kw` or `py_str` for downstream color mapping. The scanner employs a "decisive" rule to hold back tokens like identifiers, numbers, and quote sequences until enough characters are available to determine their type, ensuring accuracy across fragmented inputs. Helper functions such as `_opening_delimiter` and `_scan_string` handle the nuances of triple-quoted strings and escape sequences, while a `finish` function allows the state to be reset and final pending tokens to be processed at the end of a code block.

### send_direct.py

This file provides a command-line interface for sending single requests to a local llama-server instance, serving as a Python port of an earlier bash prototype. It parses command-line arguments to configure request parameters such as port, temperature, max tokens, system and user prompts, and optional flags for streaming, thinking, and logging. The script constructs a `Request` object using a helper function that can format file contents into a numbered block if a prompt file is specified. It then establishes a connection to the server and either streams the response through a rendering pipeline that applies syntax highlighting and formatting, or retrieves the complete response at once. The final output is directed to stdout, a specified file, or formatted as full JSON depending on the user's selections, while also optionally recording telemetry data to a JSONL file.

### server.py

This file defines the `LlamaServer` class, which serves as a Python transport layer for communicating with a `llama-server` instance in either single-model or router mode. It provides methods for non-streaming completions, raw streaming via Server-Sent Events (SSE), and a high-level `stream` method that acts as a T-junction to simultaneously aggregate response frames into a `Completion` object and emit channel-text events to any object conforming to a `Renderer` protocol. The module also includes helper methods for querying available models, retrieving server properties, and determining maximum context sizes by parsing launch arguments, while establishing a specific error taxonomy with `LlamaUnreachable` and `LlamaServerError` exceptions to distinguish between connection failures and server-side error responses.

### stages.py

This file defines a chain of renderer stages used to process and display streaming text completions from an LLM server. It establishes a base `Stage` class that implements a feed/flush pattern for passing events downstream, with specific subclasses handling distinct aspects of the output. The `ToolProgress` stage converts raw tool name and argument events into a single, throttled progress line that updates in place, while the `PyHighlight` stage uses a streaming scanner to re-channel Python code tokens into specific color-coded channels. The `Terminal` stage acts as the final sink, writing text to standard output with appropriate ANSI color codes based on the channel and TTY status, and includes logic to handle carriage returns for line redrawing. Additionally, the `Seam` stage inserts a separator between reasoning and content sections, and the `CodeFence` stage parses markdown code blocks from the content stream, re-channeling their bodies into `code` or `code_py` channels and buffering partial fence markers to handle stream fragmentation correctly.

### tokens.py

This module implements token accounting logic for managing context window budgets during LLM interactions, distinguishing between pre-completion estimation and post-completion pricing. It provides a simple `estimate_tokens` function that approximates token counts using a character-to-token ratio of four, which is used when actual tokenizer data is unavailable. The primary `turn_tokens` function calculates the precise token cost of a completed conversation turn by processing the server-provided usage metadata, subtracting known prior context tokens from the total prompt tokens to isolate the user message contribution, and proportionally allocating completion tokens to the visible assistant content versus hidden reasoning content. This calculated cost is returned to store on the Turn object for history budgeting, falling back to the character heuristic if usage data is missing or yields non-positive values, ensuring that every non-empty turn receives a positive token price.

### wire.py

The file defines the data structures and helper functions for handling chat completions from a llama-server instance, specifically modeling the request payload, the settled completion object, and the parsing of Server-Sent Events (SSE). It contains a `Request` dataclass that constructs the JSON body for the `/v1/chat/completions` endpoint, managing optional fields like model selection, tool definitions, and streaming options based on specific server requirements. The `Completion` dataclass represents the final result of a chat interaction, providing class methods to instantiate itself either directly from a non-streaming JSON response or by folding a sequence of parsed SSE frames into a coherent object, while also including a method to serialize back into the standard chat completion dictionary format. Additionally, the module includes a frozen `ToolCall` dataclass to represent individual function calls within a completion and two pure functional helpers, `parse_sse` and `events`, which respectively convert raw SSE lines into JSON dictionaries and extract channel-specific text events for rendering purposes.


## src/desh_chat

### __init__.py

The file contains no code or content, serving as an empty marker to designate its parent directory as a Python package.

### cli.py

This file serves as the main entry point for a discrete-event simulation (DES) based chatbot, defining the `main` function that parses command-line arguments to configure the application's runtime environment. It establishes a connection to a local LlamaServer instance, constructs a `Settings` object for inference parameters, and initializes an `InferenceEngine` and `ChatState` to manage the conversation history and system configuration. The script includes a `build_tools` helper that dynamically registers optional tools such as file reading, writing, editing, bash execution, time retrieval, and a delegate mechanism for spawning subagents, allowing for additive toolset configuration via CLI flags. Additionally, it features a `resolve_session_file` function that handles session persistence by determining the correct file path based on user-provided flags or auto-generating a unique identifier for new runs. After printing a formatted configuration summary to the console, the script executes the `Engine` with the prepared state, seeding the event loop with session loading and user prompt events to begin the interactive chat session.

### coding.py

This file defines the core toolset for a coding agent, centering on a frozen `Workspace` dataclass that encapsulates a project root and enforces path confinement. The `Workspace` class provides a `path` method that resolves relative paths against the root, rejecting any path that escapes the directory or traverses symbolic links, thereby ensuring all file operations remain within the specified boundary. It exposes four primary tool methods: `read`, which retrieves file contents with optional line offsets and limits; `write`, which creates or overwrites files and generates necessary parent directories; `edit`, which performs exact string replacements with logic to handle unique matches or multiple occurrences via a `replace_all` flag; and `bash`, which executes shell commands in the project root with a mandatory natural-language `reason` argument and a configurable timeout, capturing stdout, stderr, and exit codes. The file also includes an `edit_preview` helper function that generates a colorized unified diff for user confirmation during edits, and a `coding_registry` factory function that instantiates a `ToolRegistry`, binding the workspace methods to specific tool names, designating `Read` as an unconfirmed operation while requiring confirmation for `Write`, `Edit`, and `Bash`, and configuring the `Bash` tool’s identity to track only the command string rather than the reason.

### commands.py

This module defines the command system for the chat interface, establishing a central registry and event structure that allows user inputs to be parsed, validated, and executed within the application's loop. It introduces a `Command` event class that inherits from a base `Event` type and contains the raw command name and arguments, along with an `execute` method that dispatches the command while catching specific `CommandError` exceptions to return user-friendly warnings rather than crashing. The file specifies a `CommandSpec` dataclass to hold metadata for each command, including its description, aliases, and the corresponding handler function, which are aggregated into a `COMMANDS` dictionary that serves as the single source of truth for completion, help listings, and dispatch. Within the `Command` class, individual handler methods are implemented for various operations such as toggling auto or thinking modes, adjusting context windows, token limits, tool rounds, and temperature settings, as well as listing or switching models, compacting history, displaying history, and exiting the chat. These handlers utilize helper methods for argument parsing that enforce constraints on integer and float values and validate argument counts, ensuring that inputs meet the expected format before modifying the `ChatState` or emitting subsequent events like `Info`, `Warn`, `Exit`, or `MaybeRegenerate`. Finally, a `_COMMAND_INDEX` is constructed to map both canonical command names and their aliases to their respective specifications, facilitating case-sensitive dispatch during execution.

### delegate.py

This file implements a `Delegate` tool that allows a primary AI agent to offload self-contained subtasks to a temporary subagent, returning only the final answer to the parent to manage context window size. The module defines the `Delegate` dataclass, which holds configuration and the `delegate` method that the model invokes with parameters for the task, context, success gate, and optional shell check command. When executed, the tool constructs a new `ChatState` for the subagent using a specialized system prompt that instructs the agent to work independently within the project directory, and it runs a fresh `Engine` instance synchronously. During the subagent's execution, standard output is redirected through a `Gutter` context manager to visually indent the subagent's streaming output and tool interactions, distinguishing them from the parent agent's activity. The file also includes helper functions to generate unique session file names for subagent logs, fold the task brief in the parent's history to save space, and extract the final answer from the subagent's state, handling various termination conditions such as context overflow, operator cancellation, or hitting the tool round cap. Additionally, it supports a post-hoc verification step where a specified shell command is run in the project root, and its exit code and output are appended to the subagent's final answer before it is returned to the parent model.

### display.py

The file `display.py` defines a collection of frozen dataclass events that serve as terminal sinks in the application's event graph, specifically designed to output formatted text to the console without modifying the internal chat state or generating subsequent events. These classes, including `Info`, `Warn`, `Error`, `DisplayHistory`, and `DisplayStats`, all inherit from a `DisplayEvent` base class that assigns them a high execution priority to ensure their output appears before the next processing step begins. Each event implements an `execute` method that prints its message using color-coded utilities from the rendering module, with specific logic for `DisplayHistory` to iterate through conversation turns and color-code user versus assistant lines, and for `DisplayStats` to calculate and display current context window usage relative to the configured limit along with session token counts.

### events.py

This file implements the core chat loop of the application by defining a set of immutable event dataclasses that form a strongly connected graph for managing user interaction, model inference, tool execution, and context management. It begins with prompt and regeneration events that handle user input, command parsing with readline-based autocompletion, and the decision to either exit, continue an automated subagent run, or prompt for the next user message. The turn logic is structured as a multi-step process where a user message opens a pending turn, which then iterates through rounds of streaming model completions; when the model requests tool calls, the system records the round, checks for round caps or repetitive loops, and executes the calls sequentially while managing operator confirmation, denial, and cancellation states. Each tool execution updates the pending turn with results, optionally folding arguments for cache efficiency, before requesting the next round or ending the turn. Once a turn concludes, the pending state is frozen into the persistent history, triggering a compaction check that summarizes the conversation if token usage exceeds a threshold, and finally returns control to the regeneration point to await further input or idle events.

### gate.py

This module implements the operator-facing confirmation and display logic for tool invocations, serving as the human interface layer that sits between the execution engine and the user. It defines a frozen `Answer` dataclass to capture operator decisions—including approval, denial with optional feedback, cancellation, or enabling auto-mode—and provides the `ask` function, which presents a terminal prompt using raw keypress handling to allow immediate responses without requiring Enter. The file also contains utility functions for formatting tool output: `shorten` truncates and indents long text for display, while `describe_call` renders a `ToolCall` object into a human-readable string, either by delegating to a tool-specific preview method or by formatting the parsed JSON arguments with color-coded keys and values, falling back to raw strings if parsing fails. By exposing these functions and the `DENIED_TEXT` and `SKIPPED_TEXT` constants, the module allows the broader system to standardize how tool calls are presented for approval and how refusals are communicated back to the model.

### handlers.py

This file defines event handlers and custom event classes for the desh_chat application, specifically managing the behavior of the "auto" mode and error or interrupt conditions. It introduces the `AutoOff` event class, which extends the base `Event` type to modify the application state by setting the `auto` flag in the settings to false when executed. The module also provides an `on_error` function that prints the traceback of any caught exception and returns a list containing a `MaybeRegenerate` event, suggesting a retry or regeneration action after a failure. Additionally, it implements an `on_interrupt` function that handles user interruption signals, such as Ctrl+C; if the application is currently in auto mode, it returns a sequence of events to display an informational message, trigger the `AutoOff` event to disable auto mode, and allow for regeneration, whereas in standard mode, it displays an interruption message and returns an `Exit` event to terminate the application.

### session.py

This file implements session persistence for a chat application by defining two high-priority events, `LoadSession` and `SaveSession`, which manage the restoration and atomic writing of conversation history to a file path specified in the application state. The `LoadSession` event attempts to deserialize and restore chat turns from the session file at startup, handling missing files by starting a new session and corrupt files by moving them aside with a `.bad` extension to preserve the original path for future writes. Conversely, `SaveSession` serializes the current history along with metadata such as the model and system prompt to a temporary file before atomically replacing the existing session file to prevent data loss during crashes. A helper function named `persist` is also provided to generate the necessary `SaveSession` event whenever the history changes, ensuring the session file remains synchronized with the in-memory state.

### state.py

This file defines the immutable state structures and data models for a chat-based language model interaction system, serving as the central hub for managing conversation context, tool execution states, and token budgeting. It establishes a frozen `ChatState` dataclass that aggregates configuration settings, a `ChatHistory` object, inference engine details, and a `PendingTurn` to represent the transient state of an active interaction before it is finalized. The module introduces a hierarchical structure for tool interactions, using `Round` and `ToolResult` dataclasses to capture intermediate steps where the model requests tools and receives responses, which are then aggregated into a `PendingTurn` that tracks the user’s initial message and the sequence of completed tool rounds. Once a turn concludes, it is converted into a `Turn` object that includes the user’s prompt, the assistant’s final response, and metadata such as token counts and cancellation flags, which are subsequently stored in the `ChatHistory`. The `ChatHistory` class provides utilities for managing the conversation window, including methods to calculate token usage, retrieve messages since the last summary, and perform compaction by replacing older turns with a synthetic summary turn to manage context length. Additionally, the file defines a `Settings` dataclass for controlling model parameters like temperature and context limits, and includes serialization logic via `to_dict` and `from_dict` methods across all major data structures to support session persistence in a versioned format.

### toolset.py

The `toolset.py` file defines the default set of tools provided by the chat-des application, establishing that each tool is a plain function whose schema is derived by `Tool.define` from its signature and Google-style docstring to prevent drift between the model's instructions and the function's accepted arguments. It specifies that tools require operator confirmation before execution unless explicitly registered with `confirm=False`, a flag that declares the tool as read-only. The module implements a single tool function, `current_time`, which accepts an optional integer for timezone offset hours and returns the current date and time as an ISO 8601 string with second-level precision. Additionally, it provides a `default_registry` function that returns a `ToolRegistry` instance initialized with the `current_time` tool registered in read-only mode, serving as the standard configuration for available tools in the project.

